{% extends '@EasyAdmin/page/content.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <style>
        .register-wrapper { margin: 5em 20em; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        .login-wrapper {flex-direction: column; text-align: center; width: 330px;
            background-color: var(--white);
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow-lg);}

        .btn-register { color: #fff; background-color: #6174d1; border-color: #6174d1; display: block; width: 100%; padding: 7px 12px; font-size: 1rem; line-height: 1.5; border-radius: var(--border-radius);
        }
    </style>
    <div class="register-wrapper">
        <div class="login-wrapper">

            {{ form_start(visitForm) }}
            {{ form_row(visitForm.startDate) }}
            {{ form_row(visitForm.doctor) }}
            <div class="form-group field-button">
                <button type="submit" class="btn-register">Register</button>
            </div>
        </div>
    </div>
    {{ form_end(visitForm) }}
{% endblock %}